
<%= form_for @user, :url => {:controller => 'confirmations', :action => 'update'} do |f| %>

<%= f.label "Verifcation Code" %>
<%= f.text_field :verification_code %>

<%= f.label "New Password" %>
<%= f.password_field :password %>

<%= f.submit %>

<% end %>
