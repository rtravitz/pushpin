<div class="container">
  <div class="row">
    <div class="form col-md-4 col-md-offset-4">
      <h1>Sign up</h1>
      <%= form_for(@user, html: { class: "directUpload", data: {'form-data' => (@s3_direct_post.fields), 'url' => @s3_direct_post.url, 'host' => URI.parse(@s3_direct_post.url).host} }) do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.label :username %>
        <%= f.text_field :username %>
        <%= f.label :email %>
        <%= f.text_field :email %>
        <%= f.label :location %>
        <%= f.text_field :location %>
        <%= f.label :phone %>
        <%= f.text_field :phone %>
        <%= f.label :password %>
        <%= f.password_field :password %>
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation %>
        Please select a Requester or Professional account:
        <%= f.collection_select(:roles, @roles, :id, :title, include_blank: true) %>
        <%= f.label :image %>
        <%= f.file_field :image %>
        <%= f.submit "Submit", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
