<%= @proposal.id %>

<%= link_to "Award This Proposal", edit_requester_proposal_path(@proposal), method: :get %>
<div class="container">
  <div class="col-md-8">
    <h3>Proposal ID: <%= @proposal.id %></h3>
    <h4>Project Name: <%= @proposal.project.name %></h4>
    <h4>Professional: <%=link_to @proposal.user.name, professional_path(@proposal.user) %></h4>
  </div>
  <%= render partial: 'shared/message_center' %>
</div>
